import React from 'react';

export default (props) => {
  return(
    <div style={{display: 'flex', justifyContent: 'center'}}>
      <h1 style={{}}>Post no longer exists, may have been deleted.</h1>
    </div>
  );
}